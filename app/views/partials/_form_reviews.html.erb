<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content, label: 'Review', placeholder: 'Leave a review...' %>
  <%= f.input :rating, label: 'Rating', placeholder: '0 to 5.'%>
  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>
